___
1. Let $R$ be a commutative ring with identity and let $I$ be an ideal of $R$. Then prove that $I$ is a maximal ideal of $R$ if and only if $R/I$ is a field.
___
**PROOF**)
$(\Rightarrow)$
We want to show that $R/I$ is a field.
Since $I$ is ideal, $R/I$ is obviously ring. What we want to show is that for every nonzero cosets in $R/I$ has multiplicative inverse.
Suppose not and let the coset $(a+I)\in R/I$ does not have the multiplicative inverse in $R/I$.
그래서 그 뒤로 어케함 어렴풋이 기억나는건 principal이랑 엮는건가

$(\Leftarrow)$
We want to show that $I$ is a maximal ideal.
So, let $N$ is an ideal of $R$ and $I \subseteq N \subseteq R$.
We want to show that $N$ is $I$ or $N$ is $R$.
그래서 그 뒤로 어케함 principal이랑 엮는건가

___
3. Find $[\mathbb{Q}(\sqrt{3}, ^3\sqrt{5}, ^5\sqrt{7}) : \mathbb{Q}]$.
___
**SOL**)
$[\mathbb{Q}(\sqrt{3}, ^3\sqrt{5}, ^5\sqrt{7}) : \mathbb{Q}] = [\mathbb{Q}(\sqrt{3}, ^3\sqrt{5}, ^5\sqrt{7}) : \mathbb{Q}(\sqrt{3}, ^3\sqrt{5})][\mathbb{Q}(\sqrt{3}, ^3\sqrt{5}) : \mathbb{Q}(\sqrt{3})][\mathbb{Q}(\sqrt{3}) : \mathbb{Q}]$
So, $5\times 3\times 2 = 30$. (맞는지 모르겠)
그래서 이 뒤로 어케함
___
5. Factor the integer $2023$ into a product of irreducibles in $\mathbb{Z}[i]$.
___
**SOL**)

___
