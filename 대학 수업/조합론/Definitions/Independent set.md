___

**DEF**) Given a graph $G = (V, E)$, we call a set of vertices such that no two of which are adjacent is an independent set of $G$.

___

In the view of graph [[Coloring]], the set of vertices with the same color does not adjacent, so this set is independent set.

So, We can interpret independent set as the generalization of [[Color class]].