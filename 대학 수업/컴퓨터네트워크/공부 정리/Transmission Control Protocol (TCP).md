
> [!gpt] TCP가 뭔지 간략하게 설명해줘.
> TCP (Transmission Control Protocol)는 네트워크에서 데이터를 안정적으로 전송하기 위한 프로토콜입니다. TCP는 인터넷 프로토콜 스위트(IP 프로토콜 체계)의 핵심 프로토콜 중 하나로, 신뢰성 있는 데이터 전송을 보장합니다.
>
>TCP는 연결 지향적이며, 데이터의 전송 순서를 보장하고, 데이터의 손실을 최소화하기 위한 다양한 메커니즘을 제공합니다. 이를 위해 TCP는 데이터의 분할, 전송, 재조립, 흐름 제어, 오류 복구 등의 기능을 수행합니다.
>
>TCP는 데이터를 패킷 단위로 분할하여 전송하고, 수신자가 데이터를 정상적으로 받았음을 확인하는 확인 응답(ACK) 메시지를 사용하여 데이터의 신뢰성을 보장합니다. 또한 TCP는 흐름 제어 기능을 통해 송신자와 수신자 간의 데이터 전송 속도를 조절하여 네트워크 혼잡을 방지하고 데이터 전송의 신뢰성을 유지합니다.
>
>TCP는 웹 브라우징, 이메일, 파일 전송 등 다양한 응용 프로그램에서 사용되며, 안정적이고 신뢰성 있는 데이터 통신을 지원하는 데 큰 역할을 합니다.
* Motivation
	- It provides a highly reliable inter-process communiation between hosts in packet-switched communication networks
	- Connection-oriented
	- Reliable, in-sequence delivery of a full-duplex stream of octets

> [!gpt] 
> 해당 문장은 TCP의 동작과 목적을 설명합니다:
>
>1. **높은 신뢰성의 프로세스 간 통신**: TCP는 패킷 스위칭 통신 네트워크에서 호스트 간에 신뢰성있는 통신을 제공합니다. 이는 데이터가 정확하게 전송되며 손실이나 손상 없이 전달되도록 합니다.
>
>2. **연결 지향성**: TCP는 통신 시작 전에 통신하는 호스트 간에 연결을 설정합니다. 이 연결 지향적인 접근 방식은 양쪽이 동기화되어 데이터를 신뢰성 있게 교환할 준비가 되어 있는지를 보장합니다.
>
>3. **신뢰성 있는 순차 전달**: TCP는 데이터 패킷을 보낸 순서대로 전달하는 신뢰성을 보장합니다. 이는 수신 측이 전송된 데이터를 정확하게 재구성할 수 있도록 보장하며 통신 스트림의 무결성을 유지합니다.
>
>4. **전이중 통신**: TCP는 전이중 통신을 지원하여 통신하는 호스트 간에 양방향으로 데이터를 동시에 전송할 수 있습니다. 이를 통해 신뢰성을 해치지 않고 효율적인 양방향 데이터 교환을 가능하게 합니다.
>
>요약하면, 이 문장은 TCP의 목적과 특징을 강조하여 TCP의 주요 동작 및 목표를 설명합니다.

> [!gpt] full-duplex stream이 무슨 뜻이야?
> "Full-duplex stream"은 네트워크 통신에서 사용되는 용어로, 양방향으로 동시에 데이터를 전송할 수 있는 통신 형태를 의미합니다.
>
>전이중(full-duplex) 통신은 송신자와 수신자가 동시에 데이터를 주고받을 수 있는 통신 방식을 말합니다. 이는 데이터가 양쪽 방향으로 동시에 흐를 수 있으므로, 송신자와 수신자 간의 통신이 동시에 이루어지고 서로간의 통신 차단이 없습니다.
>
>이러한 통신 방식은 TCP와 같은 프로토콜에서 사용되며, 양쪽 호스트 간의 데이터 전송이 독립적으로 이루어질 수 있도록 하여 효율적이고 빠른 통신을 가능하게 합니다.

> [!gpt] packet-switched communication networks이 무슨 뜻이야?
> "Packet-switched communication networks"는 데이터 통신에 사용되는 네트워크 형태 중 하나를 의미합니다. 이는 데이터를 여러 개의 작은 패킷으로 분할하여 전송하는 네트워크입니다.
>
>패킷 스위칭 네트워크에서는 데이터가 전체 경로를 통해 한 번에 전송되는 것이 아니라, 작은 패킷 단위로 분할되어 네트워크를 통해 전송됩니다. 이 패킷들은 독립적으로 전송되며, 각 패킷은 목적지로 도달하기 위해 네트워크의 다양한 경로를 통해 전송될 수 있습니다.
>
>이러한 패킷 스위칭 네트워크의 장점은 다음과 같습니다:
>
>1. **효율성**: 패킷 스위칭은 네트워크 자원을 효율적으로 활용할 수 있습니다. 여러 개의 패킷이 동시에 전송될 수 있으며, 네트워크의 여러 경로를 통해 동시에 전송될 수 있습니다.
>
>2. **신뢰성**: 패킷 스위칭은 데이터의 전송 과정에서 발생하는 오류에 대해 복구할 수 있습니다. 각 패킷은 독립적으로 전송되므로, 하나의 패킷이 손실되더라도 나머지 패킷은 정상적으로 전송될 수 있습니다.
>
>3. **유연성**: 패킷 스위칭 네트워크는 다양한 종류의 데이터 및 트래픽을 처리할 수 있습니다. 다양한 종류의 데이터를 작은 패킷으로 분할하여 전송할 수 있으며, 네트워크의 다양한 경로를 통해 데이터를 전송할 수 있습니다.
>
>패킷 스위칭 네트워크의 대표적인 예로는 인터넷이 있으며, 이러한 네트워크는 현대의 데이터 통신에서 널리 사용되고 있습니다.
