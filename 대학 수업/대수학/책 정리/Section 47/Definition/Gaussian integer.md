___

**DEF**)
**A Gaussian integer ring** is $\mathbb{Z}[i] = \{a + bi | a, b \in \mathbb{Z}\}$.
The element $a + bi \in \mathbb{Z}[i]$ is called a **Gaussian integer**.
The norm of a Gaussian integer $N(\alpha) = a^2 + b^2$.
That is, $N(\alpha) = \alpha \times \bar{\alpha}$.

___
