___

**THEOREM**)
Let simple $G = (V, E)$ is given.
$\bar{G} = (V_{\bar{G}}, E_{\bar{G}})$, where complement of $G$ is given.
Then,
1. $V_{\bar{G}} = V_G$
2. $v$ and $w$ are adjacent in $G$ $\iff$ $v$ and $w$ are not adjacent in $\bar{G}$.

$(A_G + A_{\bar{G}})_{ij} = 0$ when $i = j$, $1$ when $i \neq j$



if $G$ is simple $k$-regular with $n$ vertices, then $\bar{G}$ is $n-1-k$-regular.
eigenvalue of $\bar{G} = n - 1 - k$

if $G$ is simple $k$-regular then $k$ is an eigenvalue of $A_G$.
$\deg (v) = k$ for all $v \in V$.

$G$ : $k$-regular
let $v$ be an eigenvector of $A_G$ and $\lambda$ be an eigenvalue of $A_G$.
$A_{G_v}v = \lambda v$

...

**THEOREM**)
If the eigenvalues of $A_G$ are $(k, \theta_2, \theta_3, \cdots, \theta_n)$, then the eigenvalues of $A_{\bar{G}}$ are $(n-1-k, -1-\theta_2, -1-\theta_3,\cdots,-1-\theta_n)$.

**PROOF**)
By $A_G + A_{\bar{G}} + I_n = J_n$, $A_{\bar{G}} = J_n - I_n - A_G$.
So, $A_Gv_i = \theta_i v_i$ $\Rightarrow$ $A_{\bar{G}}v_i = (J_n - I_n - A_G)v_i$ $=$ $J_nv_i - v_i - A_Gv_i$ $=$ $J_nv_i + (-1 - \theta_i)v_i$.
We know that $A_G$ is symmetric, so, orthogonally diagonalizable.
$v_i$ is all orthogonal to $v_1 = (1, 1, \cdots, 1)$.
So, $J_nv_i = 0$.

$G = C_6, C_5, C_4$ 일때의 eigenvalue와 complement?

$A_G$ and $B_{\bar{G}}$ $G$ and $L(G)$ $G$ and $\bar{G}$

**DEF**)
$G$ is simple graph.
$(\triangle _G)_{ij} = \deg (v_i)$ if $i = j$ else $0$
Then, $L_G = \triangle _G - A_G$ is called the Laplacian matrix of $G$.

**THEOREM**)
$L_G$ is PSD ($\lambda \geq 0$)

**DEF**)
$B_G ^O$ = ($|E| \times |V|$ sized matrix)
oriented incidence matrix
when if $a < b$ and $v_a$ and $v_b$ are incident, $a = -1, b = 1$. else $0$.

**PROOF**)
Consider $[(B_G ^O)^T(B_G ^O)]_{ij}$.

**THEOREM**)
$\mathrm{Null}(L_G)$ = the number of connected components in $G$.

___

