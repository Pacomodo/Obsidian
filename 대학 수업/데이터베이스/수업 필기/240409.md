![[Pasted image 20240409150139.png|center|400]]
트랜잭션이 Commited 되었다 = Commit log 기록이 stable storage에 잘 기록되었다.
![[Pasted image 20240409150255.png|center|400]]
Buffer manager가 빈공간을 찾으려는데 없는 경우 $\to$ 기존 블록을 없앰.
없앤 블록이 업데이트된 블록이라면, 디스크에 결과를 저장해야됨.
no-force policy : 업데이트 되어진 블록이라도, 디스크에 언제 쓰일지는 모름.
steal policy : commit 안된 트랜잭션이라도 디스크에 쓰여질 수 있다.
![[Pasted image 20240409150722.png|center|500]]
1. $T_{0}$는 commit되었다.(stable log에 쓰여짐.) 그럼에도 불구하고, $T_{0}$에 의해 변경된 X블록은 언제 업데이트 될지는 모름.
2. Commit이전에도 업데이트 될 수 있다
![[Pasted image 20240409151221.png|center|500]]
![[Pasted image 20240409151427.png|center|500]]
로그 레코드는 메인 메모리에 잠시 머무른다.(log recordd buffering)
로그 버퍼에 로그 레코드 한 블럭이 차는 경우(기록 코스트 줄이기 위함) 또는 로그 포스(강제로 로그 기록해)가 실행될 때 스토리지에 저장됨.
로그 포스 1. 트랜잭션을 커밋하기위해, 2. checkpoint manager, DB buffer manager가 요청할 때 수행됨.
![[Pasted image 20240409151836.png|center|500]]
로그 레코드는 로그가 생성된 순으로 저장된다. 트랜잭션이 커밋되었다는 뜻은 로그에 커밋되었습니다~라는게 저장될 때이다. 데이터베이스에 한 블럭을 업데이트 하려면, 그 데이터가 있는 블록과 관련된 모든 로그들을 stable storage에 저장해야만 한다.
![[Pasted image 20240409152247.png|center|500]]
![[Pasted image 20240409152351.png|center|400]]
![[Pasted image 20240409152422.png|center|400]]
![[Pasted image 20240409152445.png|center|400]]
![[Pasted image 20240409152503.png|center|400]]
![[Pasted image 20240409152611.png|center|400]]
![[Pasted image 20240409153137.png|center|500]]
트랜잭션이 중간에 실패 $\to$ atomicity를 위해 그 트랜잭션이 수행했던 연산들을 다시 복구시켜줘됨.(로그로 복구)
시스템이 중간에 다운됨(system failure) $\to$ 업데이트 한 데이터가 디스크에 반영이 안되어 있을 수 있기 때문에, redo undo작업을 해줌.
![[Pasted image 20240409153623.png|center|500]]
redo나 undo를 할 때에도 DB buffer manager에 써줌.
redo작업의 대상(abort, commit된 트랜잭션) redo는 작업한 log record를 남기지 않음.
compansion log - undo와 짝이 되는 log
![[Pasted image 20240409154437.png|center|500]]
![[Pasted image 20240409154529.png|center|500]]
$T_{1}$이 failure됨 $\to$ $T_{1}$ 롤백.
$T_{3}$가 끝난 시점에서 system failure. 그러면 첫번째 로그부터 끝까지 redo.
redo작업을 할 때 undo할 대상들을 찾는다.
그 과정은, 쭉 읽다가 커밋, 또는 abort이 되면 undo할 대상이 아니다고 판단함.
마지막까지 다 redo하고, undo할 대상($T_{2}$)를 undo해준다.
redo작업을 할 땐 로그를 안 남긴다.
![[Pasted image 20240409155720.png|center|500]]
실제 DB에 반영되었는지 안되었는지 모르기 때문에 전부 redo를 하는건데...
redo는 너무 느림.
checkpoint.
현재 실행되는 모든 트랜잭션 다 중지. DB Buffer manager에게 지금까지 업데이트 된 data를 output해주라고 함.
그러면 지금까지 update된 모든 log record들을 로그 매니저가 stable storage로 저장.
그리고 checkpoint시점에 실행되고 있던 트랜잭션을 로그 리스트로 저장해줌
![[Pasted image 20240409160059.png|center|500]]
durability가 보장된다...
![[Pasted image 20240409160410.png|center|500]]
![[Pasted image 20240409160545.png|center|500]]

