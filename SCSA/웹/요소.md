	CSS : 요소의 스타일 지정. 요소의 표현을 담당한다.(ex: `<img src="" width="10px">` / `<img src="" style="width:10px">`)

스타일 종류
- 인라인스타일(inline) : `<태그 style="프로퍼티명 : 프로퍼티 값;>`
- 이너스타일시트(inner) : `<style> 선택자{프로퍼티명 : 프로퍼티 값}</style>`
- 아우터스타일시트(outer) : `<link href="외부CSS파일" rel="stylesheet">`

